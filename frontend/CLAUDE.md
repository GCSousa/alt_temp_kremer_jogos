# CLAUDE.md (Next.js + Python Serverless)

Please refer to the root `CLAUDE.md` for project structure and commands.

Inside this folder:
- **Start local Next.js dev server**: `npm run dev`
- **Start local Flask API dev server**: `python api/index.py`
- **Run python unit tests**: `python -m pytest api/tests/test_games.py -v`
